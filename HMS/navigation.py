def show_doctors(content_frame, clear_content, doctors_items):
    clear_content()
    doctors_items(content_frame)
def show_add_doctor(content_frame, clear_content, add_doctor_form):
    clear_content()
    add_doctor_form(content_frame)