import tkinter as tk

def dashboard_items(content_frame):
    tk.Label(
        content_frame,
        text="📊 Dashboard",
        font=("Segoe UI", 20, "bold"),
        bg="red"
    ).place(x= 20,y=20,width=300,height=80)
    tk.Label(
        content_frame,
        text="📊 Dashboard",
        font=("Segoe UI", 20, "bold"),
        bg="red"
    ).place(x=350, y=20, width=300, height=80)
